{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "capture",
  "description": "Permissions for the Quick Capture Bar window: hide itself, listen for its show event, and write raw captures to SQLite.",
  "windows": ["capture"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:event:default",
    "sql:default",
    "sql:allow-execute"
  ]
}
