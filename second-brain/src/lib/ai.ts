/**
 * ai.ts — Gemini Flash helpers (stub — wired in Phase 3)
 *
 * Phase 1: file exists so imports resolve; functions throw until Phase 3.
 */

export async function extractEntities(
  _rawContent: string,
  _projects: unknown[],
  _context: unknown[]
): Promise<unknown> {
  throw new Error("ai.ts: extractEntities not yet implemented (Phase 3)");
}
