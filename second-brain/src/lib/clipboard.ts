/**
 * clipboard.ts — Tier-2 AppSync bridge (stub — wired in Phase 5)
 *
 * Phase 1: file exists so imports resolve.
 */

export function packageForClipboard(_payload: unknown): string {
  throw new Error("clipboard.ts: not yet implemented (Phase 5)");
}

export function parseAppSyncResponse(_text: string): unknown {
  throw new Error("clipboard.ts: not yet implemented (Phase 5)");
}
