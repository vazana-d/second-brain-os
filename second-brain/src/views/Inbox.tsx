// Inbox.tsx — raw captures + extraction glance (Step 1 stub; content added in Phase 3)
export default function Inbox() {
  return (
    <div className="view-placeholder">
      <span className="view-icon">📥</span>
      <h2>Inbox</h2>
      <p>Your captures and their extracted items will appear here once Phase 3 is built.</p>
    </div>
  );
}
